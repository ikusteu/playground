export enum NodeEnv {
  Production = "production",
  Development = "development",
  Test = "test",
}

export enum DeployStage {
  Development = "development",
  Storybook = "storybook",
  Test = "test",
  Staging = "staging",
  Production = "production",
}

export enum Mode {
  Build = "build",
  Serve = "serve",
}
